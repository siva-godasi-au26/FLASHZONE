<%- layout('layout') %>

<div class="container">

  <% if(user){%>
    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
          </button>
          <a class="navbar-brand" href="/">Flash Zone</a>
        </div>
          <div id="navbar" class="navbar-collapse collapse">
            <header>
              <div>
                <a class="navbar-brand" href="#"><%= user.profile.name %></a>
              </div>
              <div>
                <a class="navbar-brand" href="/profile">Profile</a>
              </div>
              <div>
                <a class="navbar-brand" href="/logout">Logout</a>
              </div>
              <div>
                <a class="navbar-brand" href="/cart"><i class="bi bi-cart-fill"></i><span><%= total %></span></a>
              </div>
            </header>

        </div>
      </div>
    </nav>
   
  <% }else{ %>
    <nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
              <span class="sr-only">Toggle navigation</span>
            </button>
            <a class="navbar-brand" href="/">Flash Zone</a>
          </div>
            <div id="navbar" class="navbar-collapse collapse">
              <header>
                <div>
                  <a class="navbar-brand" href="/signup">Signup</a>
                </div>
                <div>
                  <a class="navbar-brand" href="/login">Login</a>
                </div>
                <div>
                  <a class="navbar-brand" href="#"><i class="bi bi-cart-fill"></i></a>
                </div>
              </header>

          </div>
        </div>
    </nav>
  <% } %>

    <div class="cars">
        <h1>Bikes</h1>
        <div id="cars">
            <% for(let i = 0;i< bikes.length;i++){ %>
                <dev class="carproduct">
                  <a href="bikes/<%= bikes[i]._id %>">
                    <img id ='carimg' src="<%= bikes[i].image %>" >
                    <section id="carprice">
                    <div><%= bikes[i].name %></div>
                    <div id="price">$<%= bikes[i].price %></div>
                    </section>
                  </a>
                </dev>
            <% } %>
        </div>
    </div>